export const refractionNotes = `
Use MeshTransmissionMaterial from @react-three/drei for production glass before reaching for a custom shader.
Use a custom refraction shader only when the art direction requires distortion that cannot be expressed with transmission, thickness, IOR and chromatic aberration.
`;
