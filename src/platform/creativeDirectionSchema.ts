import { z } from "zod";
const scene = z.object({ id:z.string().min(1), purpose:z.string().min(1), subject:z.string().min(1), copy:z.string().min(1), interaction:z.string().min(1), transitionIn:z.string().min(1), transitionOut:z.string().min(1) });
export const CreativeDirectionSchema = z.object({ version:z.literal(1), conceptId:z.string().regex(/^[a-z0-9-]+$/), concept:z.string().min(1), audience:z.string().min(1), promise:z.string().min(1), emotionalArc:z.array(z.string().min(1)).min(3), cta:z.string().min(1), visual:z.object({ palette:z.array(z.string()).min(1), typography:z.array(z.string()).min(1), materials:z.array(z.string()), motion:z.array(z.string()), sound:z.array(z.string()) }), constraints:z.object({ approved:z.array(z.string()), prohibited:z.array(z.string()) }), scenes:z.array(scene).min(1), successEvent:z.string().min(1) });
export type CreativeDirection = z.infer<typeof CreativeDirectionSchema>;
export function parseCreativeDirection(input: unknown): CreativeDirection { return CreativeDirectionSchema.parse(input); }
