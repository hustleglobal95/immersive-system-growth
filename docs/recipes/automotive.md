# Automotive Cinematic Reveal

Studio-to-road vehicle storytelling using orbit, dolly and detail transitions.

Apply with:

```bash
npm run recipe:apply -- automotive
```

After applying, replace the procedural hero with the project's real asset set, then tune every camera endpoint using the debug HUD. Do not preserve the demo coordinates merely because they compile.
